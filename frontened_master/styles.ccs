/* styles.css */

/* General page styling */
body {
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg, #bbdefb, #e1bee7);
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  margin: 0;
}

/* Box styling */
.container, .login-box {
  background: #f5faff; /* pale blue card */
  padding: 30px;       /* consistent padding */
  border-radius: 10px;
  box-shadow: 0 0 12px rgba(0,0,0,0.15);
  width: 320px;        /* back to original size */
  text-align: center;
}


/* Headings */
h2 {
  margin-bottom: 20px;
  color: #1976d2; /* medium blue */
}

/* Textbox styling */
input[type="text"],
input[type="password"] {
  width: 90%;
  max-width: 310px;  
  padding: 12px 14px;
  margin: 10px auto;  
  display: block;    
  border: none;
  border-radius: 25px;            /* pill / bubbly shape */
  background: #ffffff;
  box-shadow: inset 2px 2px 5px rgba(0,0,0,0.1),
              inset -2px -2px 5px rgba(255,255,255,0.8),
              0 3px 6px rgba(0,0,0,0.1); /* depth */
  font-size: 15px;
  transition: all 0.2s ease;
}

/* Hover effect */
input:hover {
  box-shadow: inset 1px 1px 3px rgba(0,0,0,0.1),
              inset -1px -1px 3px rgba(255,255,255,0.7),
              0 4px 8px rgba(0,0,0,0.15);
}

/* Focus effect (glow) */
input:focus {
  outline: none;
  border: 1px solid #64b5f6;  /* light blue border */
  box-shadow: 0 0 8px rgba(100, 181, 246, 0.7),
              inset 1px 1px 4px rgba(0,0,0,0.1);
}

/* General button styling */
button {
  width: 100%;
  padding: 12px 14px;
  margin: 10px 0;
  border: none;
  border-radius: 25px; /* pill shape */
  font-size: 15px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  box-shadow: 2px 2px 6px rgba(0,0,0,0.2),
              -2px -2px 6px rgba(255,255,255,0.5);
}

/* Login button (blue theme) */
.login-btn {
  background-color: #1565c0;
  color: #fff;
}
.login-btn:hover {
  background-color: #0d47a1;
  box-shadow: 3px 3px 8px rgba(0,0,0,0.25),
              -3px -3px 8px rgba(255,255,255,0.4);
}
.login-btn:active {
  transform: translateY(2px); /* pressed effect */
}

/* Forgot password button (neutral grey) */
.forgot-btn {
  background-color: #9e9e9e;
  color: #fff;
}
.forgot-btn:hover {
  background-color: #757575;
  box-shadow: 3px 3px 8px rgba(0,0,0,0.25),
              -3px -3px 8px rgba(255,255,255,0.4);
}
.forgot-btn:active {
  transform: translateY(2px);
}

/* Profile page buttons */

/* Primary (Refresh) */
.btn-primary {
  background-color: #1565c0; /* strong blue */
  color: #fff;
  border-radius: 25px;
  padding: 12px 14px;
  font-size: 15px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  box-shadow: 2px 2px 6px rgba(0,0,0,0.2),
              -2px -2px 6px rgba(255,255,255,0.5);
}
.btn-primary:hover {
  background-color: #0d47a1;
  box-shadow: 3px 3px 8px rgba(0,0,0,0.25),
              -3px -3px 8px rgba(255,255,255,0.4);
}
.btn-primary:active {
  transform: translateY(2px);
}

/* Neutral (Edit) */
.btn-neutral {
  background-color: #9e9e9e;
  color: #fff;
  border-radius: 25px;
  padding: 12px 14px;
}
.btn-neutral:hover {
  background-color: #757575;
}

/* Danger (Delete) */
.btn-danger {
  background-color: #e53935;
  color: #fff;
  border-radius: 25px;
  padding: 12px 14px;
}
.btn-danger:hover {
  background-color: #c62828;
}


/* Notes / Status messages */
.note {
  font-size: 12px;
  color: #555;
  text-align: center;
  margin-top: 8px;
}

/* Row under password */
.options-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 6px 0 14px;
  font-size: 14px;
}

/* Remember Me checkbox */
.remember-me input[type="checkbox"] {
  appearance: none;         /* remove default OS style */
  width: 16px;
  height: 16px;
  border: 2px solid #1565c0;
  border-radius: 4px;       /* gives a modern square look */
  display: inline-block;
  position: relative;
  cursor: pointer;
  margin-right: 6px;
}

/* checked state */
.remember-me input[type="checkbox"]:checked {
  background-color: #1565c0;
}

.remember-me input[type="checkbox"]:checked::after {
  content: "✔";
  font-size: 12px;
  color: white;
  position: absolute;
  top: -2px;
  left: 2px;
}

/* Forgot Password link */
.forgot-link {
  text-decoration: none;
  color: #1565c0;
  font-weight: 500;
  transition: color 0.2s ease;
}
.forgot-link:hover {
  color: #0d47a1;
  text-decoration: underline;
}

/* Sign up prompt under the button */
.signup-row {
  margin-top: 12px;
  font-size: 14px;
  color: #444;
}

.signup-link {
  text-decoration: none;
  color: #1565c0;
  font-weight: 600;
  margin-left: 6px;
  transition: color 0.2s ease;
}
.signup-link:hover {
  color: #0d47a1;
  text-decoration: underline;
}